# GCP & Service Account Credentials
service_account_credentials.json
.env.gcs-manager
.env.gcs-gbp-claude
.gcp-*.json
*.key
*.pem

# Automation runtime artifacts
monitor-report.txt

# Booking engine credentials (eZee + Razorpay) — never commit
api/secrets.php

# IDE
.idea/

# OS / editor cruft
.DS_Store
*.swp
*.bak

# Scratch output from SEO agent runs — never a deploy/commit target
screenshots/
.playwright-mcp/

# Regenerable API-response caches from SEO report runs — not source content
seo-reports/**/.cache/
